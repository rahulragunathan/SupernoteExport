[project]
name = "supernote-export"
version = "0.1.0"
description = "Convert Supernote .note files into Obsidian-ready PDFs and transcribed Markdown, locally."
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
